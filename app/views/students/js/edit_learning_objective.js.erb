$('.edit_learning_objective').modal('show');
$('.edit_learning_objective_body').html("<%=escape_javascript(render 'students/partial/edit_learning_objective')%>");


$('form').each(function(){
	$(this).validate();
});
 FormsIcheck.init(); 
$('.summernote').summernote();
// $('.datepicker').datepicker({ 
//     format: 'dd-mm-yyyy' }).on('changeDate', function(ev){ $(this).datepicker('hide'); });

 $('.datepicker').datepicker({ 
    format: 'dd-mm-yyyy',orientation: 'bottom auto',autoclose: true,startDate: new Date() });